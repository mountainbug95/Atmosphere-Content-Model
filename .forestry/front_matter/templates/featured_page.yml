---
label: Featured Page Section
is_partial: true
display_field: title
fields:
  - type: text
    name: section_id
    label: Element ID
  - type: text
    name: title
    label: Section Title
    description: >-
      The section title (Used by the CMS, even if it is not displayed in the
      section)
    default: Featured Page Section
    config:
      required: true
  - type: text
    name: component
    label: Component
    hidden: true
    default: featured_page.html
    config:
      required: true
